name: Test Newsletter

on:
  workflow_dispatch: # 允許手動觸發
    inputs:
      test_mode:
        description: '測試模式'
        required: true
        type: choice
        options:
          - dry-run
          - send-test-email
          - send-to-all
        default: 'dry-run'
      article_slug:
        description: '文章 Slug（留空使用最新文章）'
        required: false
        type: string
      test_email:
        description: '測試郵件地址（僅 send-test-email 模式需要）'
        required: false
        type: string
      test_lang:
        description: '測試語系（zh-TW 或 en，僅 send-test-email 模式，留空發送兩種語系）'
        required: false
        type: choice
        options:
          - ''
          - zh-TW
          - en
        default: ''

jobs:
  test-newsletter:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: List available articles
        run: |
          echo "=== 列出所有可用文章 ==="
          node scripts/test-newsletter.js --list || true

      - name: Test Newsletter (Dry Run)
        if: github.event.inputs.test_mode == 'dry-run'
        env:
          GOOGLE_SHEETS_CREDENTIALS: ${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}
          GOOGLE_SHEETS_ID: ${{ secrets.GOOGLE_SHEETS_ID }}
        run: |
          echo "=== 測試模式（Dry Run）==="
          echo "測試模式：不實際發送郵件，只顯示會發送給誰"
          echo ""
          
          if [ -z "$GOOGLE_SHEETS_CREDENTIALS" ] || [ -z "$GOOGLE_SHEETS_ID" ]; then
            echo "⚠️ Google Sheets 憑證未設置，無法讀取訂閱列表"
            echo "請在 GitHub Secrets 中設置："
            echo "  - GOOGLE_SHEETS_CREDENTIALS"
            echo "  - GOOGLE_SHEETS_ID"
            exit 1
          fi
          
          if [ -n "${{ github.event.inputs.article_slug }}" ]; then
            echo "使用指定文章: ${{ github.event.inputs.article_slug }}"
            node scripts/test-newsletter.js --dry-run --slug=${{ github.event.inputs.article_slug }}
          else
            echo "使用最新文章"
            node scripts/test-newsletter.js --dry-run --latest
          fi

      - name: Send Test Email
        if: github.event.inputs.test_mode == 'send-test-email'
        env:
          GOOGLE_SHEETS_CREDENTIALS: ${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}
          GOOGLE_SHEETS_ID: ${{ secrets.GOOGLE_SHEETS_ID }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          BLOG_URL: ${{ secrets.BLOG_URL }}
        run: |
          echo "=== 發送測試郵件 ==="
          echo "測試模式：實際發送郵件到指定 Email"
          echo ""
          
          # 設置預設 BLOG_URL（如果未設置）
          if [ -z "$BLOG_URL" ]; then
            export BLOG_URL="https://sunzhi-will.github.io"
            echo "使用預設 BLOG_URL: $BLOG_URL"
          fi
          
          # 檢查必要環境變數
          if [ -z "$GOOGLE_SHEETS_CREDENTIALS" ] || [ -z "$GOOGLE_SHEETS_ID" ]; then
            echo "❌ Google Sheets 憑證未設置"
            echo "請在 GitHub Secrets 中設置："
            echo "  - GOOGLE_SHEETS_CREDENTIALS"
            echo "  - GOOGLE_SHEETS_ID"
            exit 1
          fi
          
          if [ -z "$GMAIL_USER" ] || [ -z "$GMAIL_APP_PASSWORD" ]; then
            echo "❌ Gmail 憑證未設置"
            echo "請在 GitHub Secrets 中設置："
            echo "  - GMAIL_USER"
            echo "  - GMAIL_APP_PASSWORD"
            exit 1
          fi
          
          if [ -z "${{ github.event.inputs.test_email }}" ]; then
            echo "❌ 測試郵件地址未提供"
            echo "請在觸發工作流程時提供測試郵件地址"
            exit 1
          fi
          
          echo "測試郵件地址: ${{ github.event.inputs.test_email }}"
          echo ""
          
          # 構建命令參數
          CMD_ARGS="--test-email=${{ github.event.inputs.test_email }}"
          
          if [ -n "${{ github.event.inputs.article_slug }}" ]; then
            echo "使用指定文章: ${{ github.event.inputs.article_slug }}"
            CMD_ARGS="$CMD_ARGS --slug=${{ github.event.inputs.article_slug }}"
          else
            echo "使用最新文章"
            CMD_ARGS="$CMD_ARGS --latest"
          fi
          
          if [ -n "${{ github.event.inputs.test_lang }}" ]; then
            echo "指定語系: ${{ github.event.inputs.test_lang }}"
            CMD_ARGS="$CMD_ARGS --lang=${{ github.event.inputs.test_lang }}"
          else
            echo "未指定語系，將發送兩種語系版本（僅用於測試）"
          fi
          
          echo ""
          echo "執行命令: node scripts/test-newsletter.js $CMD_ARGS"
          node scripts/test-newsletter.js $CMD_ARGS
          
          echo ""
          echo "✅ 測試郵件已發送！"
          echo "📬 請檢查 ${{ github.event.inputs.test_email }} 的收件匣"

      - name: Send Newsletter to All Subscribers
        if: github.event.inputs.test_mode == 'send-to-all'
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          BLOG_URL: ${{ secrets.BLOG_URL }}
          GOOGLE_SHEETS_CREDENTIALS: ${{ secrets.GOOGLE_SHEETS_CREDENTIALS }}
          GOOGLE_SHEETS_ID: ${{ secrets.GOOGLE_SHEETS_ID }}
        run: |
          echo "=== 發送電子報給所有訂閱者 ==="
          echo "⚠️  警告：此操作會實際發送郵件給所有已驗證的訂閱者！"
          echo ""
          
          # 設置預設 BLOG_URL（如果未設置）
          if [ -z "$BLOG_URL" ]; then
            export BLOG_URL="https://sunzhi-will.github.io"
            echo "使用預設 BLOG_URL: $BLOG_URL"
          fi
          
          # 檢查必要環境變數
          if [ -z "$GOOGLE_SHEETS_CREDENTIALS" ] || [ -z "$GOOGLE_SHEETS_ID" ]; then
            echo "❌ Google Sheets 憑證未設置"
            echo "請在 GitHub Secrets 中設置："
            echo "  - GOOGLE_SHEETS_CREDENTIALS"
            echo "  - GOOGLE_SHEETS_ID"
            exit 1
          fi
          
          if [ -z "$GMAIL_USER" ] || [ -z "$GMAIL_APP_PASSWORD" ]; then
            echo "❌ Gmail 憑證未設置"
            echo "請在 GitHub Secrets 中設置："
            echo "  - GMAIL_USER"
            echo "  - GMAIL_APP_PASSWORD"
            exit 1
          fi
          
          # 如果指定了文章 slug，使用 sendNewsletter 函數
          # 否則使用今天的日期（與正式發文一致）
          if [ -n "${{ github.event.inputs.article_slug }}" ]; then
            echo "使用指定文章: ${{ github.event.inputs.article_slug }}"
            echo "正在發送指定文章..."
            node -e "
              const { sendNewsletter } = require('./scripts/send-newsletter.js');
              (async () => {
                try {
                  await sendNewsletter('${{ github.event.inputs.article_slug }}');
                  process.exit(0);
                } catch (err) {
                  console.error('Error:', err);
                  process.exit(1);
                }
              })();
            "
          else
            echo "使用今天的日期（正式發文方式，但測試模式下繞過檢查）"
            echo "正在發送電子報..."
            node scripts/send-newsletter.js --test
          fi
          
          echo ""
          echo "✅ 電子報發送完成！"

      - name: Test Results Summary
        if: always()
        run: |
          echo "=== 測試結果摘要 ==="
          echo "測試模式: ${{ github.event.inputs.test_mode }}"
          if [ "${{ github.event.inputs.test_mode }}" == "send-test-email" ]; then
            echo "測試郵件地址: ${{ github.event.inputs.test_email }}"
            if [ -n "${{ github.event.inputs.test_lang }}" ]; then
              echo "測試語系: ${{ github.event.inputs.test_lang }}"
            else
              echo "測試語系: 兩種語系（zh-TW + en）"
            fi
          fi
          if [ "${{ github.event.inputs.test_mode }}" == "send-to-all" ]; then
            echo "⚠️  已發送給所有訂閱者"
            echo "📌 每個訂閱者只會收到其設定語系的版本"
          fi
          if [ -n "${{ github.event.inputs.article_slug }}" ]; then
            echo "文章 Slug: ${{ github.event.inputs.article_slug }}"
          else
            echo "文章: $([ '${{ github.event.inputs.test_mode }}' == 'send-to-all' ] && echo '今天的日期' || echo '最新文章')"
          fi
          echo ""
          echo "✅ 測試完成！"
