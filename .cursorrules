# Cursor AI Rules for sunzhi-will.github.io

## 📁 文件組織規則 (File Organization Rules)

### 文檔文件 (Documentation Files)

**所有文檔文件必須放在 `docs/` 目錄下，按類別分類：**

- **安全文檔**: `docs/security/`
  - 安全評級報告: `docs/security/rating-report.md`
  - 安全審計報告: `docs/security/audit-report.md`
  - 安全改進記錄: `docs/security/improvements.md`
  - 自動化安全配置: `docs/security/automation.md`
  - GitHub 安全功能: `docs/security/github-features.md`

- **設置文檔**: `docs/setup/`
  - Disqus 設置: `docs/setup/disqus.md`
  - 其他設置文檔: `docs/setup/*.md`

- **腳本文檔**: `docs/scripts/`
  - AI 日報生成: `docs/scripts/ai-daily-generation.md`
  - 其他腳本說明: `docs/scripts/*.md`

- **清理報告**: `docs/cleanup-report.md`

**禁止在根目錄放置任何文檔文件（除了 README.md 和 README_zh.md）**

### 代碼文件組織

- **組件**: `components/` 目錄
- **應用邏輯**: `app/` 目錄（Next.js App Router）
- **工具函數**: `lib/` 目錄
- **類型定義**: `types/` 目錄
- **數據文件**: `data/` 目錄
- **內容文件**: `content/` 目錄
- **靜態資源**: `public/` 目錄
- **腳本**: `scripts/` 目錄
  - 所有腳本文件（`.js`, `.ts`, `.ps1` 等）都應該放在 `scripts/` 目錄

### 配置文件

- **Next.js 配置**: `next.config.ts` (根目錄)
- **TypeScript 配置**: `tsconfig.json` (根目錄)
- **ESLint 配置**: `eslint.config.mjs` (根目錄)
- **Tailwind 配置**: `tailwind.config.ts` (根目錄)
- **PostCSS 配置**: `postcss.config.mjs` (根目錄)
- **GitHub Actions**: `.github/workflows/` 目錄
- **Dependabot**: `.github/dependabot.yml`

## 🚫 禁止事項

1. **禁止在根目錄放置文檔文件**（除了 README.md）
2. **禁止在根目錄放置臨時文件或報告文件**
3. **禁止重複文件**（如同時存在 `CLEANUP_REPORT.md` 和 `docs/cleanup-report.md`）
4. **禁止在根目錄放置備份文件**（如 `*.backup`, `*.bak`, `*.old`）

## ✅ 文件命名規範

- **文檔文件**: 使用 kebab-case，例如 `rating-report.md`
- **組件文件**: 使用 PascalCase，例如 `BlogCard.tsx`
- **工具文件**: 使用 camelCase，例如 `blogUtils.ts`
- **配置文件**: 使用標準名稱，例如 `next.config.ts`

## 📝 創建新文件時的規則

當創建新文件時，請遵循以下規則：

1. **文檔文件** → 放在 `docs/` 目錄下的適當子目錄
2. **安全相關文檔** → `docs/security/`
3. **設置相關文檔** → `docs/setup/`
4. **腳本相關文檔** → `docs/scripts/`
5. **清理報告** → `docs/cleanup-report.md`

## 🔍 文件檢查清單

在提交代碼前，請確認：
- [ ] 沒有文檔文件在根目錄（除了 README.md）
- [ ] 所有文檔文件都在 `docs/` 目錄下
- [ ] 沒有重複文件
- [ ] 沒有臨時文件或備份文件
- [ ] 文件命名符合規範

## 🎯 優先級

1. **高優先級**: 文件必須放在正確的目錄
2. **中優先級**: 文件命名符合規範
3. **低優先級**: 文件內容組織和格式

---

**最後更新**: 2025年1月
**維護者**: SunZhi-Will

